pre-commit:
  parallel: true
  commands:
    lint-commit:
      run: bunx commitlint --from=HEAD~1 --color

    lint:
      glob: "*.{js,ts,tsx,json}"
      run: bun lint

    typescript:
      glob: "*.{ts,tsx}"
      run: bun typescript
